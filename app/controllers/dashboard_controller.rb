# Handles the main dashboard view for authenticated users.
class DashboardController < ApplicationController
  # Renders the primary dashboard page showing the user's pets and recent activity.
  def index
  end
end
